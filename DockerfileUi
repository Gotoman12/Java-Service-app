FROM maven:3.9.12-eclipse-temurin-17-alpine AS build
WORKDIR /app
COPY . /app
RUN mvn -pl ui-service -DskipTests clean package

FROM maven:3.9.12-eclipse-temurin-17-alpine
WORKDIR /app
COPY --from=build /app/ui-service/target/*.jar app.jar
EXPOSE 8084
CMD ["java", "-jar", "app.jar"]